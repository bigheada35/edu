

### Scanner
java.util.Scanner.Scanner(InputStream source)


Constructs a new Scanner that produces values scannedfrom the specified input stream. Bytes from the stream are convertedinto characters using the underlying platform's default charset.
Parameters:source An input stream to be scanned



char ch = sc.next().charAt(0);
if(ch == 'N' || ch == 'n')
	break;
	
	
	
참고 : 	
오버로딩이란,
		같은 함수 이름의 
			파라미터 Type 이나
			갯수를 달리 하는 것.
			