함수오버로딩 : 
	같은 함수 이름이면서,
	파라메터는 타입, 갯수를 다르게 해서 
	만드는 것
	
this 용도 :  
	생성자 호출 함수( -> this() ),
	변수에 this는 자기 자신의 것

String  
	주소 비교
equals,
concat,
subString( 2, 3)
		//index =  2  부터   index < 3 까지( 미만 )  
compare(str1, str2)
		// 0 : 동일
		// 양수:  str1 이 뒤, str2 앞
		// 음수:  str2 이 앞, str2 뒤
		

